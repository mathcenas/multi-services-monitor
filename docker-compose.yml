version: '3.8'

services:
  monitor:
    build: .
    ports:
      - "3008:3001"
      - "5173:5173"
    volumes:
      - monitor-data:/data
    environment:
      - NODE_ENV=production
      - PORT=3001
      - DB_PATH=/data/monitoring.db
    restart: unless-stopped

volumes:
  monitor-data:
