version: '3.8'

services:
  monitor:
    build: .
    ports:
      - "3008:3001"
      - "5178:5178"
    volumes:
      - .:/data
    environment:
      - NODE_ENV=production
      - PORT=3001
      - DB_PATH=/data/monitoring.db
    restart: unless-stopped
